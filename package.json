{
  "name": "memos",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "build:masonry": "bun build src/frontend/masonry/index.ts --outdir=dist/masonry --target=browser --format=esm",
    "build:admin": "bun build src/frontend/admin/app.ts --outdir=dist/admin --target=browser --format=esm",
    "build": "bun run build:masonry && bun run build:admin",
    "start": "bun run build && bun run src/server.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/dompurify": "^3.2.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@chenglou/pretext": "^0.0.6",
    "dompurify": "^3.4.5",
    "hono": "^4.7.10",
    "marked": "^18.0.4",
    "vanjs-core": "^1.6.0"
  }
}
